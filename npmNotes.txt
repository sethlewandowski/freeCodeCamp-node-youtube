local dependency - use only in particular project
npm i <packageName>

//global dependency - use it in any project
// npm install -g <packageName>
// sudo npm install -g <packageName> (mac)

// npm modules

// npm i <packageName> -D or --save-dev
- this is installing a package as a dev dependency only

control + c stops nodemon or running process

// npm uninstall boostrap 
- uninstalls a dependency 
